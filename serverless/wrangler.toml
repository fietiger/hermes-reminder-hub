name = "hermes-reminder-hub"
compatibility_date = "2024-01-01"
main = "worker.js"

# 数据库绑定 (Cloudflare D1)
[[d1_databases]]
binding = "DB"
database_name = "reminder-db"
database_id = "YOUR_D1_DATABASE_ID_HERE"

# 定时调度触发器：每分钟执行一次提醒检查 (Cloudflare Cron Triggers)
[triggers]
crons = ["* * * * *"]
